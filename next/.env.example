# Local development (run `npx supabase start` first)
NEXT_PUBLIC_SUPABASE_URL=http://127.0.0.1:54321
NEXT_PUBLIC_SUPABASE_ANON_KEY=<from `npx supabase status` → Publishable key>
SUPABASE_SERVICE_ROLE_KEY=<from `npx supabase status` → Secret key>
